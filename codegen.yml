overwrite: true
schema: './src/api/**/*.graphql'
documents: null
generates:
  src/generated/graphql-types.ts:
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
    config:
      namingConvention: keep
